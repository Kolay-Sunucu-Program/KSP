zincir form_veri = FORM_VERI();

YAZ form_veri;

/*
Buradaki bilgileri aldıktan sonra isterseniz zincir fonksiyonu 
ile ayıklayabilir. İsterseniz direkt olarak veritabanı işleminde 
kullanabilirsiniz.

Örneğin 
SELECT column1, column2, ... FROM tablo_ismi;

Buradaki sütun isimleri yukarıdaki form verisinden gelebilir.
*/

//Örnek sorgulama : Tabiki bağlantı gibi işlemleri gerçekleştirdikten sonra
zincir sorgu = "SELECT " + form_veri + " FROM tablo_ismi";
