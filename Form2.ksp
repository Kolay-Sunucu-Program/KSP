zincir form_veri2 = FORM_VERI();

YAZ form_veri2;

/*
Buradaki bilgileri aldýktan sonra isterseniz zincir fonksiyonu 
ile ayýklayabilir. Ýsterseniz direkt olarak veritabaný iþleminde 
kullanabilirsiniz.

Örneðin 
SELECT column1, column2, ... FROM tablo_ismi;

Buradaki sütun isimleri yukarýdaki form verisinden gelebilir.
*/

//Örnek sorgulama : Tabiki baðlantý gibi iþlemleri gerçekleþtirdikten sonra
zincir sorgu = "SELECT " + form_veri2 + " FROM tablo_ismi";